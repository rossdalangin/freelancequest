<IfModule mod_rewrite.c>
    RewriteEngine On
    # Automatically rewrite requests to public folder if accessed directly from project root
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
